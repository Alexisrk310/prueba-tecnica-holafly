{
    "builds": [
        {
            "src": "src/api/server.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/api/$1"
        }
    ]
}
